---
description: Synchronizuj editace v wiki/ zpět do TS data souborů
---

Spusť wiki-sync agenta — propíše změny z `wiki/` (Obsidian vault) do `data/models/*.ts`, `data/lessons/*.ts`, `data/tracks.ts` a `data/courses.ts`. Detekuje, co se změnilo od posledního sync, a stručně to nahlásí.
